[{"uuid})": "uuid = uuid ?? const Uuid();\n\n  /// Parses a WireGuard configuration string into a Profile.\n  /// \n  /// [config] - The WireGuard configuration string\n  /// [name] - Optional name for the profile (defaults to interface name)\n  /// \n  /// Returns a [Result] containing the parsed [Profile] on success\n  Result<Profile> parse(String config", "section": {"section": {"id": "uuid.v4()", "name": "name ?? interfaceConfig.name", "interfaceName": "interfaceConfig.name", "privateKey": "interfaceConfig.privateKey", "publicKey": "interfaceConfig.publicKey", "listenPort": "interfaceConfig.listenPort", "mtu": "interfaceConfig.mtu", "dns": "interfaceConfig.dns", "peers": "peers", "createdAt": "DateTime.now()", "updatedAt": "DateTime.now()", ")}": ""}, ")}": ""}, "Invalid configuration": ""}, {"currentFields.add({key": "value"}, {"privatekey": "privateKey = value;\n            break;\n          case 'address':\n            address = value;\n            break;\n          case 'listenport':\n            listenPort = int.tryParse(value);\n            break;\n          case 'mtu':\n            mtu = int.tryParse(value);\n            break;\n          case 'dns':\n            dns.addAll(value.split('", ").map((s) => s.trim()).toList());\n            break;\n        }\n      }\n    }\n\n    // Validate required fields\n    if (privateKey == null || privateKey.isEmpty) {\n      return Result.failure('Interface section must contain a PrivateKey": ""}, {"TODO": "Implement actual WireGuard public key derivation\n    final publicKey = _derivePublicKey(privateKey);\n\n    return Result.success(InterfaceConfig(\n      name: address", "privateKey": "privateKey", "publicKey": "publicKey", "listenPort": "listenPort", "mtu": "mtu", "dns": "dns", "publickey": "publicKey = value;\n            break;\n          case 'presharedkey':\n            presharedKey = value;\n            break;\n          case 'endpoint':\n            endpoint = value;\n            break;\n          case 'allowedips':\n            allowedIps.addAll(value.split('", ").map((s) => s.trim()).toList());\n            break;\n          case 'persistentkeepalive": "persistentKeepalive = int.tryParse(value);\n            break;"}, {"id": "uuid.v4()", "publicKey": "publicKey", "presharedKey": "presharedKey", "endpoint": "endpoint", "allowedIps": "allowedIps", "persistentKeepalive": "persistentKeepalive", "TODO": "Implement actual WireGuard public key derivation\n    // For now", "BBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBB=": ""}, {}]